<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cjy.lamplight.dao.OrderDao">

	<!-- public Order getOrder(int id); -->
	<select id="getOrder" resultType="Order">
		SELECT *
		FROM `order`
		WHERE id
		= #{id}
	</select>

	<select id="getForPrintOrder" resultType="Order">
		SELECT O.*,
		IFNULL(M.nickname, "탈퇴회원") AS extra__member
		FROM `order` AS O
		LEFT JOIN
		`member` AS M
		ON O.clientId = M.id
		WHERE O.id = #{id}
	</select>


	<select id="orders" resultType="Order">
		SELECT *
		FROM `order`
		ORDER BY id DESC
	</select>


	<select id="getForPrintOrders" resultType="Order">
		SELECT O.*,
		IFNULL(M.nickname, "탈퇴회원") AS extra__member
		FROM `order` AS O
		LEFT JOIN `member` AS M
		ON O.clientId = M.id
		ORDER BY O.id DESC
	</select>
	
	<select id="getForPrintOrdersByMemberId" resultType="Order">
		SELECT O.*,
		IFNULL(M.nickname, "탈퇴회원") AS extra__member
		FROM `order` AS O
		LEFT JOIN `member` AS M
		ON O.clientId = M.id
		WHERE O.clientId = #{memberId}
		OR O.directorId = #{memberId}
		ORDER BY O.id DESC
	</select>

	<!-- public void addOrder(String title, String body); -->
	<insert id="addOrder" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO `order`
		<set> <!-- SET 대신 set으로 감싸주는 것이 관례 -->
			regDate = NOW(),
			updateDate = NOW(),
			option1 = #{option1},
    		option1qty = #{option1qty},
    		option2 = #{option2},
    		option2qty = #{option2qty},
    		option3 = #{option3},
    		option3qty = #{option3qty},
    		option4 = #{option4},
    		option4qty = #{option4qty},
    		option5 = #{option5},
    		option5qty = #{option5qty},
    		title = #{title},
    		funeralHome = #{funeralHome},
    		`body` = #{body},
    		`directorId` = #{directorId},
    		`clientId` = #{clientId}
		</set>
	</insert>

	<!-- public void deleteOrder(int id); -->
	<delete id="deleteOrder">
		DELETE FROM `order`
		WHERE id = #{id}
	</delete>

	<!-- public void modifyOrder(int id, String title, String body); -->
	<update id="modifyOrder">
		UPDATE `order`
		<set>  <!-- set으로 감싸는 이유? 만약 set안의 내용이 채워지지 않았을 경우 다음 단계 진행 안하고 멈춤(쿼리 오류 방지) -->
			updateDate = NOW(),
			option1 = #{option1},
    		option1qty = #{option1qty},
    		option2 = #{option2},
    		option2qty = #{option2qty},
    		option3 = #{option3},
    		option3qty = #{option3qty},
    		option4 = #{option4},
    		option4qty = #{option4qty},
    		option5 = #{option5},
    		option5qty = #{option5qty},
    		title = #{title},
    		funeralHome = #{funeralHome},
    		`body` = #{body},
    		`directorId` = #{directorId},
    		`clientId` = #{clientId}
		</set>
		WHERE id = #{id}
	</update>

	<!-- public Board getBoard(@Param("id")int id); -->
	<select id="getBoard" resultType="Board">
		SELECT *
		FROM board
		WHERE id = #{id}
	</select>

</mapper>